spring:
  datasource:
    url: jdbc:postgresql://url/account?useSSL=false
    username: username
    password: password
    driver-class-name: org.postgresql.Driver
    hikari:
      max-lifetime: 1800000

server:
  port: 8080